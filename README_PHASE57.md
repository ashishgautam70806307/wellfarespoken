# Phase 57 - Home live cleanup + single-color mobile footer nav

Changes:
- Removed duplicate hero stats cards like Daily / Basic+ from home page.
- Replaced dummy hero subtitle fallback with live-ready institute copy.
- Added frontend fallback so old dummy subtitle from DB is replaced automatically.
- Mobile footer navigation icon/font color remains #1a3565.
- Mobile footer navigation icon backgrounds now use one premium light color only: #edf3fb.
- Multiple colored icon backgrounds removed by CSS override.
