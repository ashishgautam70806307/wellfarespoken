# Application
# APP_RUNTIME_MODE=auto detects local/live. Set local or live only if hosting detection needs an explicit override.
APP_RUNTIME_MODE=auto
# APP_ENV may be omitted. The project detects local/live automatically.
APP_DEBUG=false
# Random 32+ byte application secret. Used for encrypting sensitive values such as MFA secrets.
# Example generation: php -r "echo bin2hex(random_bytes(32)), PHP_EOL;"
APP_SECRET_KEY=
APP_REMOTE_FONTS=true
APP_URL=
APP_NAME="Well Fare English Spoken"
APP_TAGLINE="Speak English With Confidence"
APP_PHONE="+91 9506617831"
APP_WHATSAPP=919506617831
APP_EMAIL=wellfareenglishspoken@gmail.com
APP_ADDRESS="Station Road, Mariahu, Jaunpur, Uttar Pradesh"

# Database connection
# auto: localhost/127.0.0.1/::1 uses DB_LOCAL_*; any other host uses DB_LIVE_*.
# manual: DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASS are used.
DB_CONNECTION_MODE=auto

DB_LOCAL_HOST=localhost
DB_LOCAL_PORT=3306
DB_LOCAL_NAME=wellfare_english
DB_LOCAL_USER=root
DB_LOCAL_PASS=

DB_LIVE_HOST=localhost
DB_LIVE_PORT=3306
DB_LIVE_NAME=
DB_LIVE_USER=
DB_LIVE_PASS=
# IMPORTANT: live credentials belong only in the server .env file. Never commit or share them.

# Generic DB_* values are read only when DB_CONNECTION_MODE=manual.
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=wellfare_english
# DB_USER=root
# DB_PASS=

SESSION_NAME=wellfare_session
# Omit SESSION_SECURE_COOKIE for automatic HTTPS detection.
SESSION_SAMESITE=Lax
# On the live HTTPS domain set this explicitly to true. If omitted, HTTPS is auto-detected.
# SESSION_SECURE_COOKIE=true
TRUST_PROXY_HEADERS=false

# Keep false during normal use. Import sql/wellfare_english_complete.sql instead.
APP_ALLOW_SCHEMA_UPDATES=false
APP_AI_TEACHER_ENABLED=false


# Student self-registration
# open = mobile + password creates an active learning account immediately (no OTP).
# approval = account is created but cannot login until an admin activates it.
STUDENT_REGISTRATION_MODE=open

# First administrator setup
# On live servers, admin/setup.php requires this secret while no admin account exists.
# Use a long random value, finish setup once, then rotate/remove it from the environment.
ADMIN_SETUP_KEY=

# Optional free TOTP MFA for administrator accounts (Authenticator apps)
ADMIN_MFA_ISSUER="Well Fare English Spoken"
# Recommended true on production. When enabled the protected Super Admin must configure Authenticator MFA.
ADMIN_REQUIRE_OWNER_MFA=true

# Optional AI enhancement. Secrets/endpoints are server environment only and never stored from Admin forms.
OPENAI_API_KEY=
OPENAI_API_ENDPOINT=https://api.openai.com/v1/chat/completions
OPENAI_ALLOWED_HOSTS=api.openai.com

# Private learning-file storage. Defaults to <project>/storage/private.
# PRIVATE_STORAGE_PATH=/absolute/private/path
