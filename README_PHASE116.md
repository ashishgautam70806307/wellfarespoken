# Phase 116 - Roadmap Lesson Word Badge Clipping Fix

Fixed roadmap lesson word cards where long words like `Itself`, `Least`, `Nothing` were being clipped inside the blue badge.

Changes:
- blue word badge is now auto-width instead of fixed 52px
- font size reduced and responsive
- chips and card text adjusted for better mobile visibility
- no PHP/database logic changed
