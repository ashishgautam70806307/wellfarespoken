# Phase 156 — Compact PWA Footer Fix

- Fixed the reusable `wf_button()` missing `type` warning.
- Replaced the large footer PWA card with one compact Install Web App button.
- Existing PWA install logic, manifest and service worker flow are preserved.
- No database or business-logic changes.
- Service-worker cache: v156.
