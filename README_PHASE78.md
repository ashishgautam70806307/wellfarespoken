# Phase 78 - Roadmap Rebuild Focused on User Requirements

Fixed:
- Replaced previous bad roadmap design with a real mobile-learning-app style layout.
- Roadmap now looks like an app shell with progress bar, energy points, section pills, locked nodes and OPEN buttons.
- Lesson page has app-like Learn / Practice / Finish tabs.
- Practice stays inside the same lesson and uses only roadmap_items of current lesson.
- Complete level shows flower/confetti celebration and unlocks the next lesson.
- Admin roadmap rebuilt into only 3 clear sections:
  1. Word Meaning
  2. Uses / Modal Pattern
  3. Tense
- Admin includes topic create, record add/edit, CSV import, manage records, checkbox multi-delete.
- No confusing 6-tab admin structure.

CSV order:
key, col1, col2, col3, col4, col5, type/tag, notes, sort_order
