# Phase 165
Current focus: restore the simple published Upcoming Test flow by default while preserving batch labels for reporting, and enforce a no-hint rule on student-facing question papers/results. Strict batch access is now opt-in instead of silently blocking legacy students.
